<h3><%= @exercise.name %></h3>
<p><%= @exercise.description %></p>

<div class="d-flex">
  <%= link_to 'Back', :back, class: 'btn btn-dark mx-1' %>
  <%= link_to 'Edit', edit_exercise_path(@exercise), class: 'btn btn-dark mx-1' %>
  <%= button_to 'Destroy', exercise_path(@exercise), method: :delete, class: 'btn btn-danger mx-1' %>
</div>
